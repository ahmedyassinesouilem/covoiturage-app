{% extends 'base.html.twig' %}

{% block title %}Hello ConducteurController!{% endblock %}

{% block body %}
<style>
    .example-wrapper {
        margin: 1em auto;
        max-width: 800px;
        width: 95%;
        font: 18px/1.5 sans-serif;
    }
    .example-wrapper code {
        background: #F5F5F5;
        padding: 2px 6px;
    }
</style>

<!-- Navbar -->
<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="#">BEST DRIVER</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="#">Home <span class="sr-only">(current)</span></a>
                
               
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{path('app_covoiturage_disponible')}}">covoiturages disponible</a>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link" href="{{path('app_mes_reservations')}}">mes reservation</a>
            </a>
               
            </li>
        </ul>
        {% if app.user %}
        <span class="navbar-text ml-auto">
            Bonjour {{ app.user.name }}! 
            <a href="{{ path('app_login') }}" class="btn btn-outline-danger btn-sm ml-2">Déconnexion</a>
        </span>
        {% endif %}
    </div>
</nav>

<!-- Main content -->
<div class="container mt-5">
    <h1>Welcome to the Conducteur Dashboard</h1>
    <p>Content for conducteurs...</p>
</div>

{% endblock %}
